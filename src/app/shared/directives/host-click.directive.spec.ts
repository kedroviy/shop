import { HostClickDirective } from './host-click.directive';

describe('HostClickDirective', () => {
  it('should create an instance', () => {
    const directive = new HostClickDirective();
    expect(directive).toBeTruthy();
  });
});
